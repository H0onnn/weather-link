import SecondaryButton from './SecondaryButton';
import WarnButton from './WarnButton';

const Button = () => null;

Button.Secondary = SecondaryButton;
Button.Warn = WarnButton;

export default Button;
